class Lobby {
  String name;
  String description;
  int iconIndex;
  int code;
  String id;

  Lobby({
    required this.name,
    required this.description,
    required this.iconIndex,
    required this.code,
    required this.id,
  });
}